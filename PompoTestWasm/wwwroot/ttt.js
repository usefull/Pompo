//////////////////////////////////////////
// The factory for transmit .NET objects.
// Generated by Pompo 03.03.2025 19:49:34.
//////////////////////////////////////////

window.transmitFunc = (obj) => {
    window.dotNetObjectFactory = obj;
    
    window.dotNetObjectFactory.create_PompoTestWasm_SomeClient = async (i) => {
        let o = null;
        window.transmitFunc = (obj) => o = obj;
        await window.dotNetObjectFactory.invokeMethodAsync(
            'Create_PompoTestWasm_SomeClient',
            i
        );

        o.Term2 = async (i, str) => await foo.invokeMethodAsync(
            'Term2',
            i, str
        );

        o.Term1 = async (i, str) => await foo.invokeMethodAsync(
            'Return',
            i, str
        );

        o.Term = async (i, str) => await foo.invokeMethodAsync(
            'Term',
            i, str
        );

        delete window.transmitFunc;
        return o;
    };
};

export function transmit(obj) {
    if (window.transmitFunc) {
        window.transmitFunc(obj);
    }
};
